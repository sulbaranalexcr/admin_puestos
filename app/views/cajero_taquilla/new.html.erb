<%= form_for @taquilla, url: @url, role: :form, html: { multipart: true } do |f| %>
  <div class="row">
    <div class="col-xs-12">
      <div class="box">

        <div class="box-header">
          <h3 class="box-title">Nuevo Cliente</h3>
        </div>

        <div class="box-body">
          <%= render partial: 'taquillas/form', locals: { f: f } %>
        </div>

        <div class="box-footer">
          <%= link_to 'Cancelar', taquillas_path, class: 'btn btn-danger' %>
          <%= f.submit 'Enviar', class: "btn btn-success", id: "boton_grabar", disabled: true %>
        </div>

      </div>
    </div>
  </div>
<% end %>
